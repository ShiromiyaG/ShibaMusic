package com.shirou.shibamusic.util

import android.content.Context

object Flavors {
    fun initializeCastContext(context: Context) {
        // Cast support is provided only in specific product flavors.
        // Default implementation is a no-op.
    }
}
